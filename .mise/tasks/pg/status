#!/usr/bin/env bash
#MISE description="Check local PostgreSQL server status"

PGDATA="${PGDATA:-.postgres}"

if [ ! -d "$PGDATA" ]; then
  echo "PostgreSQL not initialized (no $PGDATA directory)"
  exit 1
fi

pg_ctl -D "$PGDATA" status
